
language: python

python:
  - "3.8"

install:
   - pip install -r requirements.txt
   - pip install black
   - python -m pip install flake8
   - pip install coverage
   - pip install coveralls

script:
  # run black code formatter
  - black .

  # run flake8 linter
  - flake8 .

  # coverage
  - coverage run manage.py test polls

after_script:
  - coveralls

deploy:
  provider: elasticbeanstalk
  access_key_id:
    secure: "Encrypted <access-key-id>=AKIATHL5DR2XUVVXWSEE"
  secret_access_key:
    secure: "Encypted <secret-access-key>=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"
  region: "us-west-2"
  app: "django-tutorial"
  env: "django-env"
  bucket_name: "django-tutorial-bucket"

